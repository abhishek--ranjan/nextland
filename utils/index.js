// Utility functions for Nextland micro CMS
// Example: export function formatDate(date) { ... }

// Export image path utilities
export { 
  getImagePath, 
  getModeFromConfig, 
  getCategorizedImagePath, 
  validateImageExists, 
  getImageWithFallback,
  IMAGE_CATEGORIES 
} from './imagePath';
